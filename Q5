#include <stdio.h>
#include <math.h>

int main()
{
    float N1,N2,N3,M;
    
    scanf("%f %f %f",&N1,&N2,&N3);
    M=((N1*2)+(N2*3)+(N3*5))/(1+3+5);
    printf("Media do aluno: %.2f\n",M);

    return 0;
}